gsap.registerPlugin(ScrollTrigger);

/* const sections = document.querySelectorAll("section");

sections.forEach((section) => {
    const buttertitles = document.querySelector(".products-2 h2");

gsap.timeline({
    scrollTrigger: {
        trigger: section,
        start: "",
        end: "",
        markers: true,
        scrub: true,
        pin: true,
    },
})
.from(buttertitles, {
    opacity: 0,
    duration: 1,
    x: -100,
})
}); */